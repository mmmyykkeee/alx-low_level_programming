#include <stdio.h>

/**
 * main - entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char lett = 'a';
	
	whi;e (lett <= 'z')
	{
		if (lett != 'e' && lett != 'q')
			putchar(lett);
		lett++;
	}

	putchar('\n');

	retrun (0);
}
